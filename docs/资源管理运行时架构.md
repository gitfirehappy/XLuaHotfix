# 资源管理运行时架构

> Phase 1-3 已完成 | 2026-04-08

## 架构总览

```mermaid
classDiagram
    direction TB

    class AssetPackageManager {
        <<Singleton>>
        -IAssetIndex _index
        -IPackageBackend _backend
        -bool USE_AB_INDEX
        +Initialize()
        +LoadByAddress~T~(address) Task~AssetHandle_T~~
        +LoadByTypeKey~T~(key, labels) Task~AssetHandle_T~~
        +LoadAssetAsync~T~(key) Task~T~
        +UnloadAsset(key)
    }

    class IAssetIndex {
        <<interface>>
        +GetKeysByLabel(label) List~string~
        +GetKeysByType(type) List~string~
        +ContainsKey(key) bool
        +GetEntryById(entryId) RuntimeAssetEntry
        +GetEntriesByAddress(address) IReadOnlyList
    }

    class IPackageBackend {
        <<interface>>
        +LoadAssetAsync~T~(key) Task~T~
        +LoadAssetSync~T~(key) T
        +UnloadAsset(key)
        +LoadAssetAsync~T~(key, entryId) Task~T~
        +UnloadByEntryId(entryId)
    }

    AssetPackageManager --> IAssetIndex : _index
    AssetPackageManager --> IPackageBackend : _backend

    class AddressableLabelsConfig
    class AddressablesBackend
    class ABAssetIndex
    class ABPackageBackend
    class ABBundleLoader
    class ABManifest

    IAssetIndex <|.. AddressableLabelsConfig : Legacy
    IAssetIndex <|.. ABAssetIndex : AB
    IPackageBackend <|.. AddressablesBackend : Legacy
    IPackageBackend <|.. ABPackageBackend : AB
    ABPackageBackend --> ABBundleLoader
    ABPackageBackend --> ABManifest
    ABBundleLoader --> ABManifest
    ABAssetIndex --> ABManifest

    class AssetResolver {
        <<static>>
        +ResolveByAddress~T~(index, address)$ ResolveResult
        +ResolveByTypeKey~T~(index, key, labels)$ ResolveResult
    }

    AssetPackageManager ..> AssetResolver : uses
    AssetPackageManager ..> HandleRegistry : uses
    AssetPackageManager ..> AssetHandle : returns
```

## Legacy vs AB 对比

```mermaid
flowchart LR
    subgraph Legacy["Legacy 路径 (USE_AB_INDEX=false)"]
        direction TB
        L1[AddressableLabelsConfig\nSO 配置] --> L2[AddressablesBackend\nAddressables API]
        L2 --> L3[Unity Addressables\n内部引用计数]
    end

    subgraph AB["AB 路径 (USE_AB_INDEX=true)"]
        direction TB
        A1[ABManifest\nJSON 清单] --> A2[ABAssetIndex\n自建索引字典]
        A1 --> A3[ABBundleLoader\nBundle I/O + 依赖解析]
        A3 --> A4[ABPackageBackend\n双层引用计数]
    end
```

| 维度       | Legacy (Addressables)                  | AB (自研)                                 |
| -------- | -------------------------------------- | --------------------------------------- |
| **索引源**  | AddressableLabelsConfig SO             | ABManifest JSON                         |
| **加载后端** | AddressablesBackend → Addressables API | ABPackageBackend → AssetBundle API      |
| **依赖解析** | Addressables 内部自动                      | ABBundleLoader 递归解析 Bundle 级依赖          |
| **引用计数** | 单层（AsyncOperationHandle）               | 双层（Bundle 级 + Asset 级）                  |
| **资源标识** | Address 唯一                             | Address 可重复，EntryId 唯一                  |
| **错误处理** | 异常 + null                              | AssetLoadError 结构化错误码                   |
| **句柄模型** | 直接返回 T                                 | AssetHandle\<T> struct + HandleRegistry |

## 双层引用计数

AB 路径采用 Bundle + Asset 双层引用计数，精确控制卸载粒度：

```mermaid
flowchart LR
    subgraph Load["加载流程"]
        L1[请求加载 Asset] --> L2{Bundle 已缓存?}
        L2 -- 否 --> L3[ABBundleLoader.LoadBundleAsync\nBundle RefCount = 1]
        L2 -- 是 --> L4[Bundle RefCount++]
        L3 --> L5[AssetBundle.LoadAsset]
        L4 --> L5
        L5 --> L6[ABPackageBackend 缓存 Asset\nAsset RefCount = 1]
    end

    subgraph Unload["卸载流程"]
        U1[UnloadAsset / Handle.Release] --> U2[Asset RefCount--]
        U2 --> U3{Asset RefCount == 0?}
        U3 -- 否 --> U4[结束]
        U3 -- 是 --> U5[移除 Asset 缓存]
        U5 --> U6[ABBundleLoader.UnloadBundle\nBundle RefCount--]
        U6 --> U7{Bundle RefCount == 0?}
        U7 -- 否 --> U8[结束]
        U7 -- 是 --> U9[AssetBundle.Unload\nunloadAllLoadedObjects=true]
    end
```

**关键规则**：

- Bundle 引用计数 = 该 Bundle 下所有已加载 Asset 的引用数之和
- Asset RefCount 归零 → 移除 Asset 缓存 → 递减 Bundle RefCount
- Bundle RefCount 归零 → `AssetBundle.Unload(true)` 连同内部 Asset 一起卸载
- 依赖 Bundle 的 RefCount 由 ABBundleLoader 在递归加载时同步递增

## 错误处理约定

内外 API 采用不同的返回类型约定：

```mermaid
flowchart LR
    subgraph Internal["内部 API"]
        I1[ABBundleLoader] -->|"返回 (AssetBundle, AssetLoadError)"| I2[ABPackageBackend]
        I2 -->|"返回 (T asset, string bundleName, AssetLoadError)"| I3[AssetPackageManager]
    end

    subgraph External["外部 API"]
        I3 -->|"HandleRegistry.Alloc\n+ 构造 AssetHandle<T>"| E1["调用方\n收到 AssetHandle<T>"]
    end
```

| 层级                                     | 返回类型                                                | 理由                               |
| -------------------------------------- | --------------------------------------------------- | -------------------------------- |
| ABBundleLoader → ABPackageBackend      | `(AssetBundle, AssetLoadError)` ValueTuple          | 内部无需句柄，直接传递错误                    |
| ABPackageBackend → AssetPackageManager | `(T, string bundleName, AssetLoadError)` ValueTuple | 需要 bundleName 注册到 HandleRegistry |
| AssetPackageManager → 调用方              | `AssetHandle<T>` struct                             | 统一外部接口，值语义零 GC                   |
| AssetPackageManager → 调用方 (Legacy)     | `AssetHandle<T>` struct                             | Legacy 路径也走 HandleRegistry，保持一致  |

**AssetLoadError 错误码**：

| Code                  | 含义                       |
| --------------------- | ------------------------ |
| NotFound              | 条目不存在                    |
| AmbiguousMatch        | Address 重复且无法消歧          |
| BundleNotFound        | Bundle 文件缺失              |
| BundleLoadFailed      | Bundle 加载失败（I/O、CRC 等）   |
| DependencyFailed      | 依赖 Bundle 加载失败           |
| AssetExtractionFailed | AssetBundle.LoadAsset 失败 |
| LoadFailed            | 通用加载失败                   |

## 核心数据结构

```mermaid
classDiagram
    direction TB

    class RuntimeAssetEntry {
        +string EntryId
        +string Address
        +string PrimaryType
        +List~string~ Labels
        +string SourcePath
        +string Group
        +bool AutoAddress
        -HashSet _normalizedLabelsCache
        +HasLabel(label) bool
        +HasAllLabels(labels) bool
    }

    class AssetHandle~T~ {
        <<struct>>
        -int HandleId
        -int Generation
        -T _cachedAsset
        -AssetLoadError _inlineError
        +T Asset
        +bool IsValid
        +AssetLoadError Error
        +Retain() AssetHandle~T~
        +Release()
    }

    class HandleRegistry {
        <<internal static>>
        -Slot[] _slots
        -int _count
        -Stack~int~ _freeList
        +Alloc(entryId, bundleName, error, callback) tuple
        +IsValid(handleId, generation) bool
        +GetError(handleId, generation) AssetLoadError
        +Retain(handleId, generation)
        +Release(handleId, generation)
        +Reset()
    }

    class HandleRegistry_Slot {
        <<Slot struct>>
        +int Generation
        +int RefCount
        +string EntryId
        +string BundleName
        +AssetLoadError Error
        +Action~string~ ReleaseCallback
    }

    class ABManifest {
        +List~ManifestAssetEntry~ AssetEntries
        +List~ManifestBundleEntry~ BundleEntries
        +string Version
        +int AssetCount
        +int BundleCount
        +Initialize()
        +GetBundleForAsset(assetIndex) ManifestBundleEntry
        +GetBundleDependencies(bundleIndex) List~int~
        +ToJson() / FromJson()
    }

    class ManifestAssetEntry {
        +string EntryId
        +string Address
        +string PrimaryType
        +List~string~ Labels
        +string SourcePath
        +string Group
        +int BundleIndex
        +ToRuntimeEntry() RuntimeAssetEntry
    }

    class ManifestBundleEntry {
        +string BundleName
        +string FileName
        +long FileSize
        +string MD5
        +List~int~ DependBundleIndices
        +int BundleType
        +List~int~ ReferencedByBundleIndices
        +List~string~ Tags
    }

    HandleRegistry *-- HandleRegistry_Slot
    AssetHandle --> HandleRegistry : delegates refcount
    ABManifest *-- ManifestAssetEntry
    ABManifest *-- ManifestBundleEntry
    ManifestAssetEntry ..> RuntimeAssetEntry : ToRuntimeEntry()
```

## 数据流

```mermaid
sequenceDiagram
    participant Caller as 调用方
    participant APM as AssetPackageManager
    participant Resolver as AssetResolver
    participant Index as IAssetIndex
    participant Backend as IPackageBackend
    participant Registry as HandleRegistry

    Caller->>APM: LoadByAddress<T>(address)
    APM->>Resolver: ResolveByAddress<T>(index, address)
    Resolver->>Index: GetEntriesByAddress(address)
    Index-->>Resolver: entries
    Resolver-->>APM: ResolveResult { Entry }
    APM->>Backend: LoadAssetAsync<T>(address, entryId)
    Backend-->>APM: asset
    APM->>Registry: Alloc(entryId, bundleName, callback)
    Registry-->>APM: (handleId, generation)
    APM-->>Caller: AssetHandle<T> { Asset, HandleId, Generation }
```

## 关键设计决策

| # | 决策                             | 理由                                      |
| - | ------------------------------ | --------------------------------------- |
| 1 | Address 允许重复，EntryId 唯一        | 同名资源多版本共存，GUID 保证内部一致性                  |
| 2 | Group 仅构建时使用，不参与运行时查询          | 运行时按 Address/Type/Labels 查询，Group 是打包概念 |
| 3 | 双层引用计数（Bundle + Asset）         | Bundle 级控制卸载粒度，Asset 级精确追踪使用            |
| 4 | AssetHandle\<T> 为 struct       | 值语义零 GC，Generation 防悬空                  |
| 5 | USE\_AB\_INDEX 一个开关控制两个维度      | 不存在"AB 索引 + Addressables 后端"组合          |
| 6 | Bundle 级依赖（无 Asset 级依赖）        | 数据模型简单，运行时递归解析开销可接受                     |
| 7 | AssetBundle.Unload(true) 引用归零时 | 连同 Asset 一起卸载，避免残留                      |
| 8 | 内部 ValueTuple / 外部 AssetHandle | 内部高效传递，外部统一安全接口                         |

