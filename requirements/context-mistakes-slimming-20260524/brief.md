# context-mistakes-slimming-20260524 Brief

Goal: merge and trim `context/mistakes/` and related context index text so the knowledge base stays aligned with current code, keeps only verified facts, and removes stale or redundant entries.

Background:
- `context/mistakes/INDEX.md` still referenced a missing `troubleshooting.md`.
- `context/mistakes/` was split across `process-pitfalls.md`, `implementation-pitfalls.md`, and `platform-performance.md`.
- The current task is docs-only; no code or runtime behavior changes.
