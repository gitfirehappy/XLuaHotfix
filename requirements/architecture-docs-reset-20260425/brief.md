# architecture-docs-reset-20260425

## 背景

当前 `context/architecture/` 只有少量总览式文档，且中英混杂，难以作为 AI 协作时的稳定架构上下文。

同时，`docs/` 中的人类文档包含设计说明、迁移方向和 TODO，不适合直接作为 AI 的“当前事实”来源。

## 目标

1. 重置 `context/architecture/`，改为面向 AI 的英文模块化架构文档。
2. 明确区分当前运行时真相与重构中的 AB 方向，避免 AI 混淆。
3. 将 XLua 第三方底层机制单独成文，和项目侧 XLua 集成拆开。

## 约束

- 只写已验证的当前事实，不把 `docs/` 中的未来设计直接当成现状。
- 不修改运行时代码行为，本次只做文档与知识索引整理。
- `docs/README.md` 不因本次重构而机械修改；只有读者职责发生变化时才更新。
