# 错误处理模块

> 返回总览：[资源管理架构文档](./资源管理架构文档.md)

> **关联代码**
>
> `Assets/FYAsset/Scripts/Shared/Runtime/RuntimeMessage.cs` · `Assets/FYAsset/Scripts/AB/Runtime/Backends/Models/HandleRegistry.cs` · `Assets/FYAsset/Scripts/Shared/Build/Editor/Shared/BuildMessage.cs` · `Assets/FYAsset/Scripts/Shared/Runtime/Contracts/IPackageBackend.cs` · `Assets/Tools/Scripts/FileHelper.cs`
>
> 最后核对：2026-07-13

## 概述

项目错误处理采用**两层统一架构**：构建时（`BuildMessage`）和运行时（`RuntimeMessage`）共享相同的设计哲学——`Severity × Code × Message` + 工厂方法。两层类型**有意分离**（不同程序集，运行时无 `Source` 字段），不共享基类。

禁止裸 `new`，所有消息通过静态工厂方法构造。

***

## 架构设计

### 两层分离

```
构建时 (Editor)                              运行时 (Runtime)
─────────────────────                        ────────────────
BuildSeverity { Warning, Error }             RuntimeSeverity { Warning, Error }
BuildMessage {                               RuntimeMessage {
    Severity : BuildSeverity                     Severity : RuntimeSeverity
    Code     : string                            Code     : string
    Message  : string                            Message  : string
    Source   : string                            Candidates : IReadOnlyList (仅 Conflict 时)
}                                            }
工厂方法:                                    工厂方法:
  Error(code, msg, source)                     Error(code, msg)
  Warning(code, msg, source)                   Warning(code, msg)
                                              + 语义化工厂:
BuildErrorCodes（按构建域扩展）                 NotFound / Conflict / TypeMismatch
                                              LoadFailed / BundleNotFound / ...
                                              RuntimeErrorCodes（12 个常量）
```

**为什么分离**：`BuildMessage` 有 `Source`（文件路径/Collector 名，用于 Editor 诊断）；`RuntimeMessage` 有 `Candidates`（Conflict 时的候选条目列表）。强行合并到共享基类属于过度工程。

**RuntimeMessage 的 [Serializable]**：支持 Unity 序列化，用于 Inspector 调试面板和热重载异常跨域传递。只读字段通过 private constructor 初始化，序列化系统通过反射写入。

**为什么 Code 用 string**：经代码审查确认，运行时无任何代码对错误码做 switch 分支。string 允许各模块定义自己的错误码而无需修改中央 enum——与构建时相同的可扩展性收益。

**为什么运行时也有 Warning**：为降级加载、重试恢复等未来消费者预留。目前零个 Warning 消费者，但 Severity 字段成本为零（一个字段），增加它不破坏现有行为。

### 错误码常量

**构建时**（`BuildMessage.cs` 中的 `BuildErrorCodes`，下表为 Collector 常用项，不是完整清单）：

| 常量 | 含义 |
|------|------|
| `SETTING_NULL` | AssetCollectionSetting 为 null |
| `NO_PACKAGES` | AssetCollectionSetting 未配置任何 Package |
| `EMPTY_PACKAGE` | Package 内未配置任何 Group |
| `EMPTY_PACKAGE_NAME` | PackageName 为空字符串 |
| `DUPLICATE_PACKAGE_NAME` | PackageName 在同一个 Setting 中重复 |
| `EMPTY_GROUP_NAME` | GroupName 为空字符串 |
| `DUPLICATE_GROUP_NAME` | GroupName 在同一 Package 内重复 |
| `EMPTY_COLLECT_PATH` | CollectPath 为空字符串 |
| `PATH_NOT_FOUND` | CollectPath 指向的目录不存在 (Warning) |
| `CROSS_PACKAGE_OVERLAP` | 不同 Package 的 CollectPath 存在重叠 |
| `SAME_PATH_CONFLICT` | 同一 Package 内两个 Collector 路径相同 |
| `RULE_NOT_FOUND` | Rule 类名无法通过反射解析 |
| `EMPTY_COLLECTOR` | Collector 扫描后采集到零个资源 (Warning) |
| `DUPLICATE_GUID` | Package 内出现重复的 Asset GUID |

**运行时**（`RuntimeMessage.cs` 中的 `RuntimeErrorCodes`）：

| 常量 | 含义 |
|------|------|
| `NOT_FOUND` | 未找到匹配条目 |
| `AMBIGUOUS_MATCH` | 多条条目匹配，需 Labels 消歧 |
| `TYPE_MISMATCH` | PrimaryType 与请求类型不兼容 |
| `LOAD_FAILED` | 底层加载操作失败 |
| `INDEX_NOT_SUPPORTED` | 索引不支持条目级查询 |
| `BUNDLE_NOT_FOUND` | Bundle 文件在磁盘上不存在 |
| `BUNDLE_LOAD_FAILED` | AssetBundle.LoadFromFile 返回 null |
| `DEPENDENCY_FAILED` | 依赖 Bundle 加载失败 |
| `ASSET_EXTRACTION_FAILED` | 从 Bundle 提取 Asset 失败 |
| `INVALID_PAYLOAD_KIND` | 请求的 PayloadKind 与资源不一致 |
| `UNSUPPORTED_OPERATION` | 当前后端不支持该操作 |
| `INVALID_ARG` | 参数无效 |

### Severity 判定规则

- **Error** = 结果不正确/不完整，继续执行会产生错误输出
- **Warning** = 检测到异常但不影响受影响资产之外的正确性

示例：`PATH_NOT_FOUND` 和 `EMPTY_COLLECTOR` 是 Warning 而非 Error——该 Collector 的问题不影响同一 Package 内其他 Collector 的正常工作。

***

## 使用指南

| 场景 | 推荐做法 | 调用方判断 |
|------|----------|------------|
| 构建校验 | 使用 `BuildMessage` 语义化工厂；模块私有错误才用通用工厂 | `ScanResult.HasErrors` |
| 运行时解析/加载 | 使用 `RuntimeMessage.NotFound/Conflict/TypeMismatch/LoadFailed` 等工厂 | `(asset, error)` 中优先检查 `error` |
| Handle 加载 | 错误由 `AssetHandle<T>.Error` 传播 | 先检查 `IsValid`，使用完调用 `Release()` |

公开 `IPackageBackend` 加载 API 不抛业务异常：失败时资源为空且 `RuntimeMessage` 非空；成功时资源非空且错误为空。内部异常必须在后端边界转换为结构化错误。

### FileHelper 错误语义

| 方法                                           | 错误行为                                                                          |
| -------------------------------------------- | ----------------------------------------------------------------------------- |
| `ReadAllBytesAsync` / `ReadAllTextAsync`     | 抛 `FileNotFoundException`（文件不存在）或 `IOException`（Android StreamingAssets 读取失败） |
| `WriteAllBytesAtomic` / `WriteAllTextAtomic` | 抛 `ArgumentNullException`（参数为 null）或 I/O 异常                                   |
| `TryDelete` / `TryDeleteDirectory`           | **永不抛异常**，失败返回 `false` + `Debug.LogWarning`                                   |

***

## 注意事项

1. **只用工厂方法，禁止裸 new**：`RuntimeMessage` 和 `BuildMessage` 的构造函数是 `private`。所有构造必须通过工厂方法。这保证 Code 字段始终使用常量、Message 格式一致。
2. **Code 用** **`RuntimeErrorCodes`** **/** **`BuildErrorCodes`** **常量**：不要在调用点硬编码 `"NOT_FOUND"` 字符串。新增错误码先在对应的 Codes 文件中添加 `const string`。
3. **不要跨层混用**：运行时代码不应引用 `BuildMessage` / `BuildSeverity`（在 Editor 程序集中）。构建时代码不应创建 `RuntimeMessage` 用于构建诊断。两层类型有意分离。
4. **Warning 预留，暂不主动使用**：运行时 `RuntimeSeverity.Warning` 已定义且工厂方法可用，但当前无消费者。只有在确实需要"成功但不理想"语义时才使用——不要为了用而用。
5. **公开 API 不抛异常**：`IPackageBackend` 的所有实现（`ABPackageBackend`、`AddressablesBackend`）必须将错误作为 `RuntimeMessage` 返回，不抛出。调用方检查 error 字段。内部方法（`LoadAssetInternal*`）也遵守此约定。
6. **`(T, RuntimeMessage)`** **元组的错误组合**：error != null 时 asset 为 null；error == null 时 asset 保证非 null。调用方可仅检查 error。
7. **HandleRegistry 引用计数保护**：同一 EntryId 的多个 Handle 共享 `_entryActiveCounts` 计数。Release 回调仅在计数归零时触发——防止 use-after-free。不要绕过 Handle 直接调用 `UnloadAsset`/`UnloadByEntryId`（除非你确认没有活跃 Handle）。
8. **FileHelper 原子写入的临时文件**：`WriteAllBytesAtomic` 使用 `{path}.tmp.{guid8}` 格式的同目录临时文件，并通过 `File.Replace` / `File.Move` 提交。普通失败会清理临时文件；进程在提交前崩溃时仍可能残留。
