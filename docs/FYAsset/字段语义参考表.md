# 字段语义参考表

> 已落地代码中所有重要字段的语义定义。
> 防止 Labels/Tags、Address/AssetPath 等易混淆字段被误用。
> 每次新增/修改核心数据结构字段时同步更新本文档。

---

## 一、高危混淆字段对照表

| 对比项 | 字段 A | 字段 B | 区别 |
|--------|--------|--------|------|
| **Labels vs Tags** | `ManifestAssetEntry.Labels` | `ManifestBundleEntry.Tags` | Labels = 资产级逻辑标签，用于运行期查询/过滤（如 "Player", "UI"）。Tags = Bundle 级下载策略标签，用于下载分组（如 "必装", "DLC-1"）。**两者不互通、不自动聚合** |
| **Address vs AssetPath** | `RuntimeAssetEntry.Address` | `CollectedAssetInfo.AssetPath` | Address = 运行期逻辑名，允许重复，是主查询键。AssetPath = 项目相对路径（如 "Assets/Textures/icon.png"），仅编辑器诊断用，不参与运行期查询 |
| **EntryId vs AssetGUID** | `ManifestAssetEntry.EntryId` | `CollectedAssetInfo.AssetGUID` | 同一值（Unity GUID）。EntryId 是运行期规范命名，AssetGUID 是构建期命名。跨结构传递时一对一映射 |
| **GroupName vs PackageName** | `CollectedAssetInfo.GroupName` | `CollectedAssetInfo.PackageName` | 层级关系：Package → Group → Collector。PackageName 是顶层包标识，GroupName 是中间层分组 |
| **FileHash vs hash** | `ManifestBundleEntry.FileHash` | `BundleInfo.FileHash` | 已统一为 PascalCase `FileHash` |
| **BundleName vs bundleName** | `ManifestBundleEntry.BundleName` | `BundleInfo.BundleName` | 已统一为 PascalCase `BundleName` |
| **PrimaryType vs BundleType** | `ManifestAssetEntry.PrimaryType` | `ManifestBundleEntry.BundleType` | PrimaryType = 单资产的 Unity 类型名（如 "Texture2D"）。BundleType = Bundle 主导类型（>80% 同类型时取该名，否则 "Mixed"） |

---

## 二、核心数据结构字段清单

### VersionNumber（版本号，全项目统一使用）

| 字段 | 类型 | 语义 |
|------|------|------|
| `Major` | int | 主版本号 — 不兼容的大版本更新，客户端必须强制更新安装包 |
| `Minor` | int | 次版本号 — 功能性更新，热更可达 |
| `Patch` | int | 修订号 — Bug 修复、资源微调，热更可达 |
| `Build` | int | 构建号（E9 计划）— 每日自增元数据，不参与版本比较，用于区分同版本多次构建 |
| `Channel` | string | 发布渠道（E9 计划）— ""(release), "alpha", "beta", "rc"。参与版本比较：alpha < beta < rc < release |

> 比较规则：Major → Minor → Patch → ChannelRank。Build 不参与。

`VersionDataBase` 是产品版本源，不按 AA / AB 拆分；AA / AB 是构建后端维度，通过 `BackendMode` 写入 PackageIndex、BuildIndexData 和 Build Repository channel。

---

### PackageIndex（远端包体入口）

| 字段 | 类型 | 语义 |
|------|------|------|
| `LatestPackage` | string | 最新包体目录名 |
| `LatestVersion` | VersionNumber | 最新包体版本 |
| `BackendMode` | string | 包体后端类型，值为 `AA` 或 `AB`；运行期必须与当前客户端后端一致 |

---

### BuildIndexData（整包启动数据）

| 字段 | 类型 | 语义 |
|------|------|------|
| `BuildGUID` | string | 整包基线包体目录名 |
| `BuildTime` | string | 构建时间 |
| `IsDebug` | bool | 是否为 Development Build |
| `Platform` | string | Unity BuildTarget |
| `BackendMode` | string | 整包基线后端类型，值为 `AA` 或 `AB` |
| `Version` | VersionNumber | 整包版本 |

---

### RuntimeAssetEntry（运行期最小资产单元）

| 字段 | 类型 | 语义 |
|------|------|------|
| `EntryId` | string | 唯一标识，复用 Unity GUID。缓存键、Handle 归属判定、诊断 ID |
| `Address` | string | 逻辑名，允许重复。由 IAddressRule 生成或手动锁定。运行期主查询键 |
| `PrimaryType` | string | 资产 Unity 类型名（如 "Texture2D", "GameObject"）。ScriptableObject 用具体子类名 |
| `Labels` | IReadOnlyList\<string\> | 无序唯一标签集。存储保留原始大小写，匹配时大小写不敏感 |
| `SourcePath` | string | 项目相对路径（如 "Assets/Prefabs/Player.prefab"）。仅编辑器诊断，运行期不使用 |
| `Group` | string | 构建组名。仅编辑器诊断，运行期不参与查询或路由 |
| `AutoAddress` | bool | true = 地址由规则自动生成（重建时可能变化）；false = 手动锁定（永不自动覆盖） |

---

### ManifestBundleEntry（Bundle 元数据）

| 字段 | 类型 | 语义 |
|------|------|------|
| `BundleName` | string | 唯一 Bundle 标识（如 "characters_assets_all_abc123.bundle"） |
| `FileHash` | string | MD5 内容哈希 — 完整性校验 + 增量更新比对 |
| `FileCRC` | uint | CRC32 校验 — 快速验证 |
| `FileSize` | long | 文件大小（字节）— 下载进度估算 |
| `BundleType` | string | 主导类型名或 "Mixed" — 由 Bundle 内资产 PrimaryType 统计推断 |
| `Tags` | List\<string\> | Bundle 级下载策略标签（如 "必装", "DLC-1", "background"）。**不是资产 Labels** |
| `DependBundleIndices` | int[] | 直接依赖的 Bundle 索引（指向 ABManifest.BundleEntries） |
| `Encrypted` | bool | 是否加密 |

---

### CollectedAssetInfo（构建中间产物）

| 字段 | 类型 | 语义 |
|------|------|------|
| `AssetPath` | string | 项目相对路径 — 构建期唯一定位 |
| `AssetGUID` | string | Unity GUID — 映射为运行期 EntryId |
| `Address` | string | 运行期地址 — 由 IAddressRule 生成 |
| `PrimaryType` | string | 资产类型名 — 由 AssetDatabase.GetMainAssetTypeAtPath 获取 |
| `Labels` | List\<string\> | 合并标签 — Group.Labels ∪ Collector.Labels，去重 |
| `GroupName` | string | 所属 Group 名 |
| `PackageName` | string | 所属 Package 名 |
| `BundleName` | string | 逻辑 Bundle 名 — 由 BundleNameBuilder 组装（格式: pkg_group_packKey） |
| `Classification` | AssetClassification | 分类结果 — Role + PayloadKind 两个正交维度 |
| `CollectorType` | ECollectorType | 采集器类型 — 透传自 Collector 配置 |
| `IsInSharedBundle` | bool | 依赖分析标记 — 资产被放入共享 Bundle（$shared 组） |
| `IsDuplicated` | bool | 依赖分析标记 — 隐式依赖被复制到多个引用 Bundle 中 |

---

### ABManifest（清单根结构）

| 字段 | 类型 | 语义 |
|------|------|------|
| `PackageName` | string | 包标识（如 "MainPackage"） |
| `PackageVersion` | VersionNumber | 构建版本号 |
| `BuildTimestamp` | string | 构建时间（ISO 8601），调试用 |
| `AssetEntries` | List\<ManifestAssetEntry\> | 所有资产条目 |
| `BundleEntries` | List\<ManifestBundleEntry\> | 所有 Bundle 条目 |

运行期索引（Initialize 后构建）：
- `_addressIndex`: Address → 资产索引列表（允许重复）
- `_entryIdIndex`: EntryId → 资产索引（唯一）
- `_typeIndex`: PrimaryType → 资产索引列表
- `_labelIndex`: Label(lowercase) → 资产索引列表
- `_bundleNameIndex`: BundleName → Bundle 索引

---

### BundleBuildInfo（构建管线中间产物）

| 字段 | 类型 | 语义 |
|------|------|------|
| `BundleName` | string | 逻辑 Bundle 名（不含 hash/后缀） |
| `OutputFileName` | string | 实际输出文件名（含 hash + .bundle 后缀） |
| `Hash` | string | Unity BuildPipeline 产出的内容哈希 — 映射为 ManifestBundleEntry.FileHash |
| `Size` | long | 文件大小（字节） |
| `AssetPaths` | List\<string\> | 此 Bundle 包含的所有资产路径 |
| `PayloadKind` | EPayloadKind | 主导载荷类型 |

---

### AAManifest（AA 包体清单）

| 字段 | 类型 | 语义 |
|------|------|------|
| `Version` | VersionNumber | 版本号 |
| `FileHash` | string | 整体比对标识 |
| `TotalSize` | long | 更新 Bundle 总大小 |
| `Bundles` | List\<BundleInfo\> | Bundle 列表 |

### BundleInfo（AA Bundle 描述）

| 字段 | 类型 | 语义 |
|------|------|------|
| `BundleName` | string | Bundle 文件名 |
| `FileHash` | string | Bundle 文件哈希 |
| `FileSize` | long | Bundle 文件大小 |

---

### Build Repository（构建仓库）

RepositoryCommit:
| 字段 | 类型 | 语义 |
|------|------|------|
| `Version` | VersionNumber | 构建版本 |
| `ChannelKey` | string | 仓库 Channel 标识，包含 BuildTarget、可选 Channel、BackendMode |
| `BackendMode` | string | 构建后端类型，当前为 AA 或 AB |
| `BuildTarget` | string | Unity BuildTarget |
| `PackageName` | string | 包体名称 |
| `CreatedAtUtc` | string | Commit 创建时间，UTC ISO-8601 字符串 |
| `Artifacts` | List\<ArtifactDigest\> | 本次构建写入 HEAD 的产物指纹列表 |

RepositoryHeadState:
| 字段 | 类型 | 语义 |
|------|------|------|
| `HeadVersion` | string | 当前 HEAD 指向的 Version，object 路径由目录布局推导 |

ArtifactDigest:
| 字段 | 类型 | 语义 |
|------|------|------|
| `Name` | string | 产物身份；AA 使用 Asset GUID，AB 使用 BundleName |
| `Hash` | string | 内容 Hash，当前使用 MD5 字符串 |
| `Size` | long | 产物大小，单位为 byte |
| `CRC` | uint | CRC32 快速校验值 |

ArtifactDelta:
| 字段 | 类型 | 语义 |
|------|------|------|
| `Added` | List\<ArtifactDigest\> | 目标侧存在、基准侧不存在的产物 |
| `Modified` | List\<ArtifactDigest\> | 两侧 Name 相同但 Hash 不同的产物 |
| `Removed` | List\<string\> | 基准侧存在、目标侧不存在的产物 Name |

---

## 三、枚举语义

### ECollectorType（采集器类型）

| 值 | 语义 |
|----|------|
| `Main` (0) | 可寻址入口资产，运行时通过 Address 加载 |
| `Static` (1) | 内部打包资产，不对外暴露 Address |
| `Depend` (2) | 仅作为依赖项打包，不直接加载 |
| `Implicit` (3) | 由依赖分析自动发现的隐式依赖资产 |

### EPayloadKind（载荷类型）

| 值 | 语义 |
|----|------|
| `Serialized` (0) | 标准序列化资产（Prefab/Texture/Material），打入 AssetBundle |
| `RawFile` (1) | 原始文件，直接拷贝，不打入 AssetBundle |
| `Scene` (2) | 场景文件，Unity 要求独立打包为 Scene Bundle |

### EAssetRole（资产语义角色）

| 值 | 语义 |
|----|------|
| `Main` (0) | 来自 Main 采集器的可寻址入口资产 |
| `Static` (1) | 来自 Static 采集器的内部打包资产 |
| `Depend` (2) | 来自 Depend 采集器的显式声明依赖资产 |
| `ImplicitDependency` (3) | 由依赖分析发现的隐式依赖资产 |

### EForcePayloadKind（强制载荷覆盖）

| 值 | 语义 |
|----|------|
| `Auto` (0) | 由 Classifier 自动推断 |
| `Serialized` (1) | 强制视为序列化资产 |
| `RawFile` (2) | 强制视为原始文件 |
| `Scene` (3) | 强制视为场景文件 |

---

## 四、BuildContext Keys（管线数据总线键）

| Key | 值类型 | 语义 |
|-----|--------|------|
| `BuildConfig` | BuildConfig | 构建环境不可变快照（BackendMode + Version + BuildVersionString + OutputRoot + TargetPlatform），TaskPrepareContext 唯一写入 |
| `CollectedAssets` | List\<CollectedAssetInfo\> | 收集阶段产出的资产列表 |
| `SharePolicies` | object | 依赖共享策略 |
| `BundleDependencyGraph` | BundleDependencyGraph | Bundle 依赖关系图 |
| `BundleBuildResults` | List\<BundleBuildInfo\> | 构建产物元数据 |
| `ABManifest` | ABManifest | 生成的清单 |
| `OutputPath` | string | 最终输出目录 |
| `BuildVerificationResult` | BuildVerificationResult | 构建验证结果 |

---

## 五、命名规范

| 规则 | 说明 |
|------|------|
| 全项目统一用 PascalCase | `EntryId`, `FileHash`, `BundleName`, `PackageName`, `Version`, `TotalSize`, `FileSize` |
| 同语义跨结构必须同名 | `PrimaryType` 在 RuntimeAssetEntry / ManifestAssetEntry / CollectedAssetInfo 中一致 |
| Labels 永远是资产级 | 不要在 Bundle 结构上使用 Labels 命名 |
| Tags 永远是 Bundle 级 | 不要在资产结构上使用 Tags 命名 |
| Hash 字段统一为 FileHash | 新代码中不再使用 `hash`、`Hash`、`ContentHash` 等变体 |

---

## 六、待统一修改点

| 旧字段 | 所在类 | 目标命名 | 状态 |
|--------|--------|---------|------|
| `version` | AAManifest | `Version` | ✅ 已改 |
| ~~`hash`~~ | ~~AAManifest~~ | ~~`FileHash`~~ | ✅ 已改 |
| ~~`hash`~~ | ~~BundleInfo~~ | ~~`FileHash`~~ | ✅ 已改 |
| ~~`bundleName`~~ | ~~BundleInfo~~ | ~~`BundleName`~~ | ✅ 已改 |
| ~~`size`~~ | ~~BundleInfo~~ | ~~`FileSize`~~ | ✅ 已改 |
| ~~`totalSize`~~ | ~~AAManifest~~ | ~~`TotalSize`~~ | ✅ 已改 |
| ~~`latestPackage`~~ | ~~PackageIndex~~ | ~~`LatestPackage`~~ | ✅ 已改 |
| ~~`latestversion`~~ | ~~PackageIndex~~ | ~~`LatestVersion`~~ | ✅ 已改 |
