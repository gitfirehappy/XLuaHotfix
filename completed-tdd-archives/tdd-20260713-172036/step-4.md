# Step 4 - Implement to Make Tests Pass

## Implementations Completed

- FR-1: Major baseline and durable active pointer - `docs/scenario/hotfix-runtime-state-machine.md` - Implemented in the shared Hotfix flow, state decider, package inspection, settings, and FileHelper.

The focused state-machine test passes. Disposable-directory checks also cover atomic replacement and CRC package inspection.
